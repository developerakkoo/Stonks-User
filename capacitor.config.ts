import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.stocks.techlapse',
  appName: 'Nifty Level Tracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
